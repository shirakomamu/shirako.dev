export enum MutationEnums {
  SET_LOADED = "setTechnologyLoaded",
  INCREMENT_ID = "incrementUniqueId",
}

export enum ActionEnums {
  COMMIT_PUSH = "commitPush",
  GET_NEXT_UNIQUE_ID = "generateId",
}
